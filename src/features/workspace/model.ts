export type AgentTemplate = { id: string; projectId: string; name: string; providerId: string | null; role: string; instruction: string; environmentFiles: string[]; unitLimit: number | null };
export type EnvironmentProfile = { id: string; projectId: string; name: string; environmentFiles: string[]; validationCommands: string[] };
export type WorkspaceSearchResult = { kind: "project" | "task" | "agent" | "log" | "event" | "review" | "commit"; id: string; projectId: string; taskId: string | null; runId: string | null; title: string; detail: string };
export type Bookmark = { id: string; projectId: string; taskId: string | null; runId: string | null; eventId: string | null; label: string; createdAt: string };
export type WorkspaceSnapshot = { id: string; projectId: string; taskId: string | null; runId: string | null; kind: "checkpoint" | "snapshot"; label: string; baseRevision: string; createdAt: string };
export type MergeQueueItem = { id: string; projectId: string; taskId: string; attemptId: string; fingerprint: string; status: "queued" | "running" | "succeeded" | "failed" | "cancelled"; resultRevision: string | null; error: string | null; createdAt: string };
export type Dashboard = { activeAgents: number; pendingTasks: number; blockedTasks: number; reviews: number; failures: number; queuedMerges: number; snapshots: number; bookmarks: number; totalReportedUnits: number; attentionItems: number; recentActivity: { eventType: string; taskId: string | null; createdAt: string }[] };
export type ExplorerAgent = { run: { id: string; taskId: string; providerName: string; title: string | null; role: string; status: string; worktreePath: string | null; port: number | null; canResume: boolean; unitLimit: number | null }; usage: { active: boolean; processId: number | null; cpuPercent: number | null; residentBytes: number | null } };
