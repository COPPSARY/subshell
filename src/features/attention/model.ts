export type AttentionItem = { key: string; reason: "blocked" | "failed" | "completed_unreviewed" | "approval_waiting"; projectId: string; taskId: string; runId: string | null; approvalRequestId: string | null; title: string; detail: string; stateFingerprint: string; acknowledged: boolean; createdAt: string };
export type ApprovalRequest = { id: string; projectId: string; taskId: string; runId: string | null; action: string; arguments: Record<string, unknown>; status: "pending" | "approved" | "denied" | "expired"; requestedBy: string; createdAt: string; decidedAt: string | null; executionStatus: "not_started" | "running" | "succeeded" | "failed"; executionResult: unknown | null; executionErrorCode: string | null; executionErrorMessage: string | null; executedAt: string | null };
